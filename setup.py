from setuptools import setup, find_packages

setup(
    name="financial_time_series_library",  # Name of the package (rename if needed)
    version="0.1.0",
    packages=find_packages(),  # Automatically find packages inside the directory
    install_requires=[],  # Add dependencies if needed
)